name: ditto_ffi_app
description: Flutter desktop app demonstrating FFI integration with native KV store library
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0
  test: ^1.26.2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  ffigen: ^19.1.0

flutter:
  uses-material-design: true
